# Required for S3 access (API + ETL)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_BUCKET_NAME=

# Optional: only needed when submitting ECS batch jobs locally
AWS_ACCOUNT_ID=
CONTAINER_NAME=
ECS_CLUSTER_NAME=
