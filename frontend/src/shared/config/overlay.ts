/**
 * Overlay configuration constants.
 *
 * Shared overlay styling values for consistent loading states
 * across the application.
 *
 * @module overlay
 */

/** Opacity for full-screen overlay backgrounds */
export const OVERLAY_OPACITY = 0.3;

/** Opacity for inner/nested overlays (more opaque to obscure content) */
export const OVERLAY_OPACITY_INNER = 0.9;

/** Vuetify theme color for overlay scrim */
export const OVERLAY_COLOR = "background";
